# Declare Ghana-specific helper variables here when an encoded rule needs a
# reusable concept that is not directly present in source law. Ghana is a
# unitary state, so all helpers are national (gh) scope.
